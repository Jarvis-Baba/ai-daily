[pytest]
# integration = hits real network / external services.
# Excluded from the default run so unit tests stay hermetic and green
# offline; run them explicitly with:  pytest -m integration
markers =
    integration: real network or external services; run with -m integration
addopts = -m "not integration"
